# レート表示の修正

## 問題
- Room → ゲーム開始フローでレート表示が表示されない
- 初期multiplierが1の場合、`multiplier > 1`の条件で非表示になっていた

## 修正内容

### 1. GameField.tsx - レート表示を常に表示
```typescript
// 修正前
{multiplier > 1 && (() => {

// 修正後
{(() => {
```

### 2. getRateStyle関数 - 1倍のスタイルを追加
```typescript
// ×1: グレー
return {
  bg: 'linear-gradient(160deg, #6b7280 0%, #4b5563 100%)',
  shadow: '0 0 8px rgba(107,114,128,0.3)',
  border: 'rgba(107,114,128,0.4)',
  labelColor: '#d1d5db',
  pulse: false,
};
```

## テスト結果

### Room → ゲーム開始フロー
- ✅ **レート表示あり** - `×1`（グレー）
- ✅ **山札枚数表示あり** - 正しい枚数

### デバッグGameボタンフロー
- ✅ **レート表示あり** - `×2`（ブルー）
- ✅ **山札枚数表示あり** - 28枚

## レート表示の色分け

| 倍率 | 色 | エフェクト |
|------|------|------------|
| ×1 | グレー | なし |
| ×2 | ブルー | なし |
| ×4 | ゴールド | なし |
| ×6 | オレンジ→レッド | なし |
| ×8以上 | 深紅 | グロー点滅 |

## 確認コマンド

```bash
npm run test:e2e tests/actual-game-flow.spec.ts
```
